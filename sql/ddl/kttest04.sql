CREATE TABLE IF NOT EXISTS `{project}.{dataset}.kttest04` (
  `rank` INTEGER,
  `peak` INTEGER,
  `all_time_peak` INTEGER,
  `actual_gross` INTEGER,
  `adjusted_gross_2022_dollars` INTEGER,
  `artist` STRING,
  `tour_title` STRING,
  `years` STRING,
  `shows` INTEGER,
  `average_gross` INTEGER,
  `ref` STRING,
  `_ingested_at` TIMESTAMP
);
