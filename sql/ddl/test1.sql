CREATE TABLE IF NOT EXISTS `{project}.{dataset}.test1` (
  `rank` INTEGER,
  `peak` INTEGER,
  `all_time_peak` INTEGER,
  `actual_gross` INTEGER,
  `adjusted_gross_in_2022_dollars` INTEGER,
  `artist` STRING,
  `tour_title` STRING,
  `year_s` STRING,
  `shows` INTEGER,
  `average_gross` INTEGER,
  `ref` STRING,
  `ingested_at` TIMESTAMP
);
