CREATE TABLE IF NOT EXISTS `{project}.{dataset}.test2` (
  `rank` INTEGER,
  `peak` INTEGER,
  `all_time_peak` INTEGER,
  `actual_gross` INTEGER,
  `adjusted_gross_in_2022_dollars` INTEGER,
  `artist` STRING,
  `tour_title` STRING,
  `years` STRING,
  `shows` INTEGER,
  `average_gross` INTEGER,
  `ref` STRING,
  `_ingested_at` TIMESTAMP,
  `_source_file` STRING,
  `_pipeline_version` STRING
);
