# Environment variables for local development.
# Copy to .env and adjust values:  cp .env.example .env

# Database connection string
DATABASE_URL=sqlite:////tmp/app.db
# Secret key for JWT token signing
JWT_SECRET_KEY=dev-secret-change-in-production
# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# Read by the backend code (STRIPE_WEBHOOK_SECRET)
STRIPE_WEBHOOK_SECRET=whsec_test_secret

# --- Auto-added at push: vars read by the code but missing above ---
# Read by the backend code (BQ_DATASET)
BQ_DATASET=analytics
# Read by the backend code (BQ_TARGET_TABLE)
BQ_TARGET_TABLE=postgres_test2
# Read by the backend code (BQ_WRITE_DISPOSITION)
BQ_WRITE_DISPOSITION=WRITE_TRUNCATE
# Read by the backend code (DB_NAME)
DB_NAME=postgres
# Read by the backend code (DB_USER)
DB_USER=559906504681-compute@developer
# Read by the backend code (GCP_PROJECT)
GCP_PROJECT=upbeat-repeater-477110-q6
# Read by the backend code (INSTANCE_CONNECTION_NAME)
INSTANCE_CONNECTION_NAME=upbeat-repeater-477110-q6:us-central1:sdlc-etl-demo-db
# Read by the backend code (POSTGRES_DB_URL)
POSTGRES_DB_URL=changeme
# Read by the backend code (SOURCE_DB_URL)
SOURCE_DB_URL=changeme
# Read by the backend code (SOURCE_TABLE)
SOURCE_TABLE=test_data
